# 原始GLM版本配置

# 模型配置
base_url: "http://localhost:4244/v1"  # 本地部署使用 4244，远程服务器可自定义端口
model: "autoglm-phone-9b"
api_key: ""  # 如果需要API key，在这里配置

# Agent配置
max_steps: 100
device_id: "your-device-id"  # autoglm顺序执行，指定设备ID
lang: "zh"  # "zh" 或 "en"
verbose: false

